describe('VSCodeWebviewBridge', () => {
  it('should build message handler', () => {

  })
  it('should get state', () => {

  })

  it('should set state', () => {
    
  })

  it('should dispose', () => {

  })
})
