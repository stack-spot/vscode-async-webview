describe('VSCodeWebViewProvider', () => {
  it('should register itself as a webview provider', () => {

  })

  it('should build the view', () => {
    
  })

  it('should enable JS and allow access to the webview\'s folder', () => {
    
  })

  it('should build bridge', () => {
    
  })

  it('should use the HTML loaded from the index file and include a <base> tag', () => {
      
  })

  it('should show the webview', () => {
    
  })

  describe('when the webview is disposed', () => {
    it('should dispose the bridge and free the references to both the view and the bridge', () => {
    
    })

    it('should get bridge async', () => {

    })
  })

})